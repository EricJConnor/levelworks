# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Note: RESEND_API_KEY should be configured as a secret in Supabase Edge Functions
# Go to: Supabase Dashboard > Project Settings > Edge Functions > Secrets
# Add: RESEND_API_KEY = your-resend-api-key
#
# The edge functions access this via Deno.env.get('RESEND_API_KEY')
# The frontend does NOT need the Resend API key - it calls the edge function
